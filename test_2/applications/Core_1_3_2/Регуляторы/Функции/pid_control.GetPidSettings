METHOD PRIVATE GetPidSettings : PidData
VAR_INPUT
	selected_number	:	WORD;
END_VAR

// === IMPLEMENTATION ===

IF selected_number > 0 AND selected_number <= GetObjectCount() THEN
	GetPidSettings:= SETTINGS.DATA.PID_SETTINGS.Number[selected_number];
END_IF